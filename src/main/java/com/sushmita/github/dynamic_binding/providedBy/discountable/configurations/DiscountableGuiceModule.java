package com.sushmita.github.dynamic_binding.providedBy.discountable.configurations;

import com.google.inject.Binder;
import com.google.inject.Module;
import com.sushmita.github.dynamic_binding.providedBy.discountable.Discountable;
import com.sushmita.github.dynamic_binding.providedBy.discountable.provider.DiscountableProvider;

public class DiscountableGuiceModule implements Module {
    public void configure(Binder binder) {
    }
}
